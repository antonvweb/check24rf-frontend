name: Deploy to Test Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Setup SSH agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Add server to known_hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ secrets.SERVER_IP }} >> ~/.ssh/known_hosts
        continue-on-error: false

      - name: Sync project to server
        run: |
          rsync -avz --delete \
            --exclude node_modules \
            --exclude .git \
            --exclude .gitignore \
            --exclude *.log \
            --exclude .env \
            ./ root@${{ secrets.SERVER_IP }}:${{ secrets.DEPLOY_PATH }}/

      - name: Build and deploy app
        run: |
          ssh root@${{ secrets.SERVER_IP }} << 'EOF'
            set -e

            cd ${{ secrets.DEPLOY_PATH }}

            echo "📦 Установка зависимостей..."
            npm ci

            echo "🔧 Сборка проекта..."
            npm run build

            echo "🛑 Завершение текущего процесса на порту ${{ secrets.APP_PORT }}..."
            pkill -f "node.*${{ secrets.APP_PORT }}" || true

            echo "🚀 Запуск приложения с PM2..."
            npm install -g pm2
            pm2 start npm --name "check24rf" -- start -- -p ${{ secrets.APP_PORT }}
            pm2 save
          EOF

      - name: Verify deployment
        run: |
          sleep 5
          response=$(curl --silent --write-out "%{http_code}" --output /dev/null http://${{ secrets.SERVER_IP }}:${{ secrets.APP_PORT }}/health || true)
          if [ "$response" -ne 200 ]; then
            echo "❌ Ошибка: приложение не отвечает (HTTP $response)"
            exit 1
          else
            echo "✅ Приложение успешно запущено"
          fi